describe('confirmation-dialog hook specs', () => {
  describe('hook tests', () => {
    it('should return true', () => {
      // Arrange
      // Act
      // Assert
      expect(true).toBe(true);
    });
  });
});
